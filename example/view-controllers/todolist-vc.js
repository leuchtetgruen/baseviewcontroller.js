var createTodoListController = function(done) {
		return BaseViewController.extend({

		});
};

var TodoListController = createTodoListController(false);
var DoneListController = createTodoListController(true);
